* Add a validation that a proposed workout cannot be deleted if a result workout is related to it
* Put the exercise entity in resultExercise and remove proposedExerciseId

* Remove dependencies from mongo
* Improve the SignupController, to save the user in a user table.
* In AuthenticationResource return the user from the database.
* Get the use from session and put it in the resultWorkout DTO.
* Update README.
* Review session duration
* Move all endpoints to /api/ and restrict /api/**
* Move the translation to each class.
* Document the API.
* Add code quality analysis on push.
