* Get the use from session and put it in the resultWorkout DTO.
* Update README.
* Change the Attribute so it is not necessary to do this: The reason is needed (although never shown) so DefaultErrorResponseAttributes can process it.
* Review session duration
* Put the exercise entity in resultExercise and remove proposedExerciseIgd
* Move all endpoints to /api/ and restrict /api/**
* Move the translation to each class.
* Document the API.
* Add code quality analysis on push.
